Act as a Senior Staff Computer Vision Engineer and Software Architect. We are building a production-grade fixed-wing UAV detection pipeline for interceptor drones, specifically targeting a resource-constrained edge device (NVIDIA Jetson Orin Nano 8GB).

Task:
Scaffold the project boilerplate and core architecture in Python. The code MUST be strictly Object-Oriented (OOP), fully type-hinted (using the `typing` module), and well-documented with Docstrings (Google or NumPy format). We are not training the model yet; focus entirely on the system architecture.

Please generate the directory structure and the following specific files based on these architectural requirements:

1. Data Contracts (`src/core/data_contracts.py`):
- Use `@dataclass` to define the data objects that will flow through our system.
- Create a `Detection` class containing: `class_id` (int), `class_label` (str), `confidence` (float), and `bbox` (normalized coordinates as a tuple or list: [x_center, y_center, width, height]).
- Create a `FrameData` class containing: `frame` (np.ndarray), `frame_id` (int), `timestamp` (float), and `detections` (List[Detection], initialized as empty).

2. Strategy & Factory Patterns (`src/models/base_detector.py` and `src/models/detector_factory.py`):
- In `base_detector.py`: Create an abstract base class (ABC) named `BaseDetector` with an abstract method `predict(self, frame: np.ndarray) -> List[Detection]`.
- Create a stub implementation named `YOLOv11Detector` that inherits from `BaseDetector` and returns mock `Detection` objects for testing.
- In `detector_factory.py`: Create a `DetectorFactory` class with a static method that takes a model name string (e.g., "yolov11", "tensorrt") and returns the corresponding instantiated detector class. This guarantees we can swap backbones later without touching the downstream logic.

3. Pipe and Filter Architecture (`src/pipeline/pipeline_manager.py`):
- Create a `PipelineManager` class to orchestrate the data flow.
- Implement a `process_video(self, video_path: str)` method. This method should read frames using OpenCV (Ingest), create a `FrameData` object, pass the frame to the detector (injected via the Factory), attach the resulting `Detection` objects to the `FrameData`, and finally pass the `FrameData` to a stub method `_suppress_false_positives(self, frame_data: FrameData)`.

4. Anti-Leakage Dataset Splitter (`tools/split_dataset.py`):
- Write a robust script to split a raw dataset of images into Train/Val/Test directories.
- CRITICAL: Do NOT use a standard random split. Implement a Group K-Fold / Sequence-based split logic. 
- The script should group images by their source video sequence (e.g., by extracting a prefix from the filename, assuming format `videoID_frameNumber.jpg`). Ensure that all frames belonging to the same `videoID` are placed entirely into either Train, Val, or Test to absolutely prevent temporal Data Leakage.

Output Requirements:
Generate the code for all the requested files, ensure imports are correct, and provide a short `README.md` explaining how to run the `split_dataset.py` script.